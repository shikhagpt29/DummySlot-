var Button = /** @class */ (function () {
        var button = new PIXI.Container();
        button.position.set(10, 137);
        button.interactive = true;
        button.buttonMode = true;
        button.name = "button";
    return button;
});